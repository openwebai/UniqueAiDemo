[project]
name = "unique-ai-demo"
version = "0.1.0"
description = "MVP demo for experimenting with the Unique AI platform SDK"
requires-python = ">=3.11"
dependencies = [
    "unique-sdk",
    "python-dotenv",
    "flask",
    "httpx",
]

[project.optional-dependencies]
dev = [
    "ruff",
]
